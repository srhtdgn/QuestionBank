@model QuestionBank.Models.Lesson
@{
    ViewBag.Page = "Dersler";
    ViewBag.Title = "Ders Yönetimi";
    ViewBag.Description = "Ders ekleme ekranı";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-md-6">

        <div class="btn-group">
            <button class="btn sbold green" onclick="location.href='@Url.RouteUrl("Lessons")'">
                Ders Listesi
                <i class="fa fa-chevron-left"></i>
            </button>
        </div>
    </div>
</div>
<br />
@Html.Raw(@ViewBag.Message)
@using (Html.BeginForm("Add", "Lesson", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.LessonName, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.LessonName, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.LessonName, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Ekle" class="btn btn-default" />
            </div>
        </div>
    </div>
}

